# Install mutint-core dependencies first:
#   pip install -r mutint-core/requirements.txt
#
# mutint-refsniff-specific additions (if any):
#
# None. The two services this plugin talks to are reached with what core already has:
# `requests` for ENA and for NCBI Datasets, and `sendsketch.sh` -- a conda tool, see
# tools.txt -- which speaks to JGI's sketch server itself.
