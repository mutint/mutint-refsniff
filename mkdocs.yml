# mutint-refsniff's documentation. This component has no entry script of its own, so these
# pages are read through an assembled project's manual -- `./mutint docs` collects them.
#
# The top-level nav heading is how a page declares who it is for. `Using MutInt` and
# `Extending MutInt` are the two the manual merges; anything under another heading lands under
# "About this deployment" named for this component. See mutint_common/docs_manual.py.

site_name: mutint-refsniff
site_description: Identifying an experiment's reference genome from a sample of its reads

nav:
  - Using MutInt:
      - Identifying a reference from reads: using/refsniff.md
